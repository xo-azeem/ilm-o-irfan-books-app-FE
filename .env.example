# Staging Supabase (public anon key only — never put service_role here)
SUPABASE_URL=https://rwnmckrepvycydmtgvcq.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here

# RevenueCat public SDK keys (Dashboard → Project → API keys). Not the webhook secret.
# Leave blank until the RC project exists — Subscribe will explain keys are missing.
REVENUECAT_API_KEY_IOS=
REVENUECAT_API_KEY_ANDROID=
