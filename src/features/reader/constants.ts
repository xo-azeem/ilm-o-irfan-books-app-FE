export const MIN_SCALE = 1;
export const MAX_SCALE = 4;
export const SCALE_STEP = 0.25;

/** Padding around the fitted PDF page so it does not kiss the screen edge. */
export const PAGE_INSET = 10;

export const FLIP_DURATION_MS = 320;
export const FLIP_COMMIT_RATIO = 0.18;
export const FLIP_COMMIT_VELOCITY = 650;
export const FLIP_EDGE_RATIO = 0.28;
export const FLIP_ACTIVE_OFFSET = 12;
export const FLIP_FAIL_OFFSET_Y = 80;
export const FLIP_MAX_ROTATE_DEG = 70;
export const FLIP_RUBBER_RATIO = 0.14;
export const FLIP_PREPARE_MS = 32;
